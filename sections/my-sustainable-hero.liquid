{% schema %}
{
  "name": "my-sustainable-hero",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "primary_image",
      "label": "Primary Image",
      "info": "Main portrait image (recommended: 600x800px)"
    },
    {
      "type": "image_picker",
      "id": "secondary_image", 
      "label": "Secondary Image",
      "info": "Overlapping accessory image (recommended: 400x300px)"
    },
    {
      "type": "text",
      "id": "eyebrow_text",
      "label": "Eyebrow Text",
      "default": "EVERYDAY ESSENTIALS"
    },
    {
      "type": "text",
      "id": "main_heading",
      "label": "Main Heading",
      "default": "Sustainable Trends You'll Love Forever."
    },
    {
      "type": "textarea",
      "id": "value_points",
      "label": "Value Points",
      "info": "Separate each point with | (pipe symbol)",
      "default": "We believe that fashion should be stylish, affordable and accessible to everyone.|Body positivity and inclusivity are values that are at the heart of our brand.|We want everyone to look and feel good, while also doing our part to help the environment."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Call to Action Text",
      "default": "OUR STORY"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Call to Action URL"
    }
  ],
  "presets": [
    {
      "name": "my-sustainable-hero"
    }
  ]
}
{% endschema %}

<div class="my-sustainable-hero__container" data-section-id="{{ section.id }}">
  <!-- Left side - Two overlapping images -->
  <div class="hero-images">
    <div class="image-primary">
      <img 
          width="400"
          height="400"
          src="{{ section.settings.primary_image | image_url: width:400 }}" 
          alt="{{ section.settings.primary_image.alt | default: 'Sustainable fashion model' }}"
          loading="lazy"
      />
    </div>
    <div class="image-secondary">
      <img
          width="400"
          height="400" 
          src="{{ section.settings.secondary_image | image_url: width:400 }}" 
          alt="{{ section.settings.secondary_image.alt | default: 'Fashion accessory' }}"
          loading="lazy"
      />
    </div>
  </div>
  <!-- Right side - Content -->
  <div class="hero-content">
    <div class="content-wrapper">
      {% if section.settings.eyebrow_text != blank %}
        <p class="eyebrow">{{ section.settings.eyebrow_text }}</p>
      {% endif %}
      
      {% if section.settings.main_heading != blank %}
        <h1 class="main-heading">{{ section.settings.main_heading }}</h1>
      {% endif %}

      {% if section.settings.value_points != blank %}
        <div class="value-points">
          {% assign points = section.settings.value_points | split: '|' %}
          {% for point in points %}
            {% if point != blank %}
              <div class="value-point">
                <span class="diamond">♦</span>
                <p>{{ point | strip }}</p>
              </div>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}

      {% if section.settings.cta_text != blank and section.settings.cta_url != blank %}
        <div class="cta-wrapper">
          <a href="{{ section.settings.cta_url }}" class="cta-link">
            {{ section.settings.cta_text }}
          </a>
        </div>
      {% endif %}
    </div>
  </div>
</div>

{{ 'my-sustainable-hero.css' | asset_url | stylesheet_tag }}

<script defer type="modul">
  (function(){
  })()
</script>