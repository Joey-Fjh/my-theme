<tab-control class="tab-container">
  <ul class="tab-titles">
    {% for block in section.blocks %}
      <li class="tab-title" 
        data-tab-index="{{ forloop.index }}"
      >
        type: {{ block.type }}<br>
        title: {{ block.settings.title | default: '无标题' }}

        {{ block.settings.title }}
      </li>
    {% endfor %}
  </ul>
  {% comment %} <div class="tab-contents">
    {% for block in section.blocks %}
      <div class="tab-content" 
        data-tab-index="{{ forloop.index }}"
      >
        {% render block %}
      </div>
    {% endfor %}
  </div> {% endcomment %}
</tab-control>

{% schema %}
  {
    "name": "Tab Control",
    "tag": "section",
    "class": "section",
    "blocks": [
      { 
        "type": "_tab-item",
      }
    ],
    "max_blocks": 4,
    "presets": [
      {
        "name": "Tab Control",
        "blocks": [
          { "type": "_tab-item" }
        ]
      }
    ]
  }
{% endschema %}