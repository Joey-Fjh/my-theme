<tab-control class="tab-container">
  <ul class="tab-titles">
    {% for block in section.blocks %}
      {% render block %}
      {% comment %} <li class="tab-title" 
        data-tab-index="{{ forloop.index }}"
      >
        {{ block.settings.title }}
      </li> {% endcomment %}
    {% endfor %}
  </ul>
  {% comment %} <div class="tab-contents">
    {% for block in section.blocks %}
      <div class="tab-content" 
        data-tab-index="{{ forloop.index }}"
      >
        {% render block %}
      </div>
    {% endfor %}
  </div> {% endcomment %}
</tab-control>

{% schema %}
  {
    "name": "Tab Control",
    "tag": "section",
    "class": "section",
    "blocks": [
      { 
        "type": "test",
      }
    ],
    "max_blocks": 4,
    "presets": [
      {
        "name": "Tab Control",
        "blocks": [
          { "type": "test" }
        ]
      }
    ]
  }
{% endschema %}