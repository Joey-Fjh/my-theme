{{ 'my-featured-blog.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign articles = section.settings.blog.articles
  assign left_article = articles.first
  assign show_image = section.settings.show_image
  assign show_date = section.settings.show_date
  assign show_author = section.settings.show_author
-%}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<responsive-container class="my-featured-blog__container section-{{ section.id }}-padding
    {% unless section.settings.is_fullWidth %} my-page-width {% endunless %}
    color-{{ section.settings.color_scheme }}
    "
>
    <div class="top">
        <h2
            id="SectionHeading-{{ section.id }}"
            class="inline-richtext {{ section.settings.heading_size }}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
            {% if settings.animations_reveal_on_scroll %}
                data-cascade
            {% endif %}
            style="text-align:{{ section.settings.text_align }};"
        >
            {{ section.settings.heading }}
        </h2>
    </div>

    <div class="bottom">
        <a class="left a-reset-block" href="{{ left_article.url }}">
            <responsive-container class="image__container">
                {%- if left_article.image != blank -%}
                    {{ 
                        left_article.image
                        | image_url: width: left_article.image.width
                        | image_tag:
                            alt: left_article.image.alt,
                            class:"left-image"
                    }}
                {%- else -%}
                   {{ 'blog-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }} 
                {%- endif -%}

                <div class="content">
                    <div class="title h2">
                        {{ left_article.title }}
                    </div>
                    <div class="description h3">
                        {{ left_article.content }}
                    </div>
                    <span class="link">Read more</span>
                </div>
            </responsive-container>
        </a>
        <div class="right">
            {%- for i in (2..articles.size) limit: 2 -%}
                {% assign idx = i | minus: 1 %}
                {% assign article = articles[idx] %}

                <a class="card a-reset-block" href="{{ article.url }}">
                    {%- if show_image -%}
                        <responsive-container class="image__container right-image-container" style="aspect-ratio: 1 / 1;">
                            {%- if article.image != blank -%}
                                {{ 
                                    article.image
                                    | image_url: width: article.image.width
                                    | image_tag:
                                        alt:article.image.alt,
                                        class:"right-image" 
                                }}
                            {%- else -%}
                                {{ 'blog-apparel-2' | placeholder_svg_tag: 'placeholder-svg' }}
                            {%- endif -%}
                        </responsive-container>
                    {%- endif -%}

                    <div class="content">
                        <div class="my-info">
                            {%- if show_date -%}
                                <span>{{ article.published_at | time_tag: format: 'date' }}</span>
                            {%- endif -%}
                            {%- if show_author -%}
                                <span>{{ article.author }}</span>
                            {%- endif -%}
                            <span>{{ article.comments_count }} comments</span>
                        </div>
                        <div class="my-title">
                            {{ article.title }}
                        </div>
                        <div class="my-description">
                            {{ article.content }}
                        </div>
                        <span class="my-link">Read more</span>
                    </div>
                </a>
            {%- endfor -%}
        </div>
    </div>
</responsive-container>


{% schema %}
  {
    "name": "my-featured-blog",
    "tag": "section",
    "class": "section",
    "disabled_on": {
        "groups": ["header", "footer"]
    },
    "settings": [
        {
            "type": "color_scheme",
            "id": "color_scheme",
            "label": "Color Scheme",
            "default": "scheme-1"
        },
        {
            "type": "checkbox",
            "id": "is_fullWidth",
            "label": "Full Width",
            "default": false
        },
        {
            "type": "header",
            "content": "Text"
        },           
        {
            "type": "inline_richtext",
            "id": "heading",
            "default": "Blog Posts",
            "label": "Title"
        },
        {
            "type": "select",
            "id": "text_align",
            "label": "Text Align",
            "options": [
                {
                    "value": "left",
                    "label": "Left"
                },
                {
                    "value": "center",
                    "label": "Center"
                },
                {
                    "value": "right",
                    "label": "Right"
                }
            ],
            "default":"center"
        },
        {
            "type": "select",
            "id": "heading_size",
            "options": [
                {
                    "value": "h2",
                    "label": "Small"
                },
                {
                    "value": "h1",
                    "label": "Medium"
                },
                {
                    "value": "h0",
                    "label": "Large"
                }
            ],
            "default": "h1",
            "label": "Text Size"
        },
        {
            "type": "header",
            "content": "Blog Card"
        },
        {
            "type": "blog",
            "id": "blog",
            "label": "t:sections.featured-blog.settings.blog.label"
        },
        {
            "type": "checkbox",
            "id": "show_image",
            "default": true,
            "label": "Show Image"
        },
        {
            "type": "checkbox",
            "id": "show_date",
            "default": true,
            "label": "Show Date"
        },
        {
            "type": "checkbox",
            "id": "show_author",
            "default": false,
            "label": "Show Author"
        },
        {
            "type": "header",
            "content": "Padding"
        },
        {
            "type": "range",
            "id": "padding_top",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "Padding Top",
            "default": 0
        },
        {
            "type": "range",
            "id": "padding_bottom",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "Padding Bottom",
            "default": 0
        }
    ],
    "presets": [
      {
        "name": "my-featured-blog"
      }
    ]
  }
{% endschema %}